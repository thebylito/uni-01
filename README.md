# Descricao workflow
```.github/workflows/azure.....yml```


Sao os eventos em que o GithubActions "escuta", no caso do ```on: [push]```, toda vez que um commit entrar na branch os jobs serão executados.
Ainda nesse contexto, temos os Jobs, que basicamente são as etapas que serão executadas quando o workflow for trigado.
